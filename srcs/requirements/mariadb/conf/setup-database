CREATE DATABASE IF NOT EXIST wordpress;
CREATE USER ${LOGIN}@% IDENTIFIED BY ${MYSQL_USER_PASSWORD};
CREATE USER 'hypervisor'@% IDENTIFIED BY ${MYSQL_ROOT_PASSWORD};
GRANT ALL PRIVILEGES ON wordpress.* TO 'wordpress'@% IDENTIFIED BY ${MYSQL_ROOT_PASSWORD};
FLUSH PRIVILEGES;